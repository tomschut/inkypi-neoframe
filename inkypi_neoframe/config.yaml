name: InkyPi NeoFrame
version: "0.1.0"
slug: inkypi_neoframe
description: InkyPi image playlists with packed Spectra-6 frames for NeoFrame firmware
arch:
  - amd64
startup: application
boot: auto
init: true
ports:
  80/tcp: 8084
ports_description:
  80/tcp: InkyPi web UI and firmware packed-frame downloads
webui: http://[HOST]:[PORT:80]/
watchdog: tcp://[HOST]:[PORT:80]
backup: cold
options: {}
schema: {}
